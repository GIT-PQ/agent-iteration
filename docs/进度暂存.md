# 进度暂存（2026-09-16，临时文件——恢复后可删）

> 用途：跨机器续接。以下为当前快照与待办。

## 仓库状态

- 未提交改动：仅 `docs/plan.md`（P-1 裁决 + P-4 更新 + §2 登记，均已当面确认）
- T-001 候选已由维护者提交：`e108098`（工件五文件 + 04）、`2ada3e3`（02 方案修订）；无 tag；commit message 未带 `[T-001]` 前缀

## T-001（git 写操作改为提醒人执行）——停在 3.4 出口

- 3.4 已完成（工件改动 + 自检通过），候选已提交 = 冻结
- 3.5 未开始。2026-09-15 执行 agent 越权补写文件链（01/02/03 + 改 04）已被回滚，**教训：文件链缺口必须上报待人裁决，不得补写**
- 待办：3.5 验证（非实现者、干净上下文、对 e108098 的 git archive 副本）→ 3.6（提醒人打 tag + changelog + 归档提交，plan.md 的未提交改动随归档一并提交；P-2 状态行「skill 层同步待执行」届时更新）

## plan.md 各条目

- **P-1**：已裁决——P2~P7 全覆盖（P3/P6/P7 超出）；P1 接收标准、经验检索、经验回流三个差距全部挂起，带重启触发条件
- **P-2**：已裁决，T-001 执行中
- **P-3**：已关闭
- **P-4**：讨论中（当前主战场，见下）

## P-4 待裁决（下次从这里继续）

五个缺口：①无活动任务指针；②plan↔iterations 链接单向；③无聚合视图；④裁决豁免无机制；⑤state_check 不看 git、跳跃误报。

已定方向：状态从工件推导，不建「当前 state」文件；终态条目可归档分区。

待裁决的方案（四点，已讨论未拍板）：

1. SKILL.md 加「裁决豁免」规则：人裁决跳过状态 → 一行占位文件（日期+裁决人+一句话），经人确认，执行 agent 不得代写
2. SKILL.md 横切规范加行为红线：守卫与现实矛盾一律上报，不得自行补写绕过（进反模式表）
3. state_check.py 升级：识别占位文件跳过推导；无豁免跳跃报异常；结合 git 判候选是否已提交；输出全任务总表
4. 3.6 归档检查措辞：「01-05 齐全（含豁免占位）」

裁决后：写入 P-4 裁决结果 → 转任务 T-002 实施（P2 级）。归因结论已确认：根因 = 守卫与进度共用文件存在性信号、豁免无表达；直接诱因 = 执行 agent 面对两难选择博弈而非上报。
